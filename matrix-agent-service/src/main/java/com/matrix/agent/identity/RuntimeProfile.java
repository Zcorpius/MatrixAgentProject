package com.matrix.agent.identity;

/** Trusted deployment class, captured on each request rather than supplied by a model or client. */
public enum RuntimeProfile {
    PHONE,
    AUTOMOTIVE,
    UNKNOWN
}
